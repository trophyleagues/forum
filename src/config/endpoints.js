import {url} from './config'

export const url_register = `${url}/register`
export const url_login = `${url}/login`
export const auth_login = `${url}/auth/login`
export const forums = `${url}/forums`
export const subforum = `${url}/subforum`
export const post = `${url}/post`
export const response = `${url}/response`
